# 設計変更メモ

## 変更ルール
- 設計書（No.1〜No.33）からの変更点をここに記録する
- 後でまとめて設計書に反映する

---

## 変更履歴

### 2026年6月 変更①
**変更箇所:** No.5 テーブル定義書：マスタ系
**変更内容:** m_companiesテーブルに事業年度開始日（日）を追加
- 変更前: fiscal_year_start_month（月のみ）
- 変更後: fiscal_year_start_month（月）＋
          fiscal_year_start_day（日）を追加
**影響する設計書:**
- No.5 テーブル定義書：マスタ系
- No.14 予算管理画面
- No.22 予算管理仕様書
**理由:** 会社によって事業年度開始日が
月初以外の場合があるため

### 2026年6月 変更②
**変更箇所:** No.5 テーブル定義書：マスタ系
**変更内容:** m_companiesに
  fiscal_year_start_day（開始日）を追加
**影響する設計書:**
- No.5 テーブル定義書：マスタ系
- No.14 予算管理画面
- No.22 予算管理仕様書
**理由:** 開始日が月初以外の会社に対応

---

## 未反映の設計書一覧
- [ ] No.5 テーブル定義書：マスタ系
- [ ] No.14 予算管理画面
- [ ] No.22 予算管理仕様書

---

## 変更③ インボイス制度対応（2026-06-07）

### 対象ファイル
- `src/data/invoiceConstants.js`（新規）
- `src/components/JournalEntry.jsx`（修正）
- `src/components/JournalList.jsx`（修正）
- `src/components/InvoiceSummary.jsx`（新規）
- `src/App.jsx`（修正）
- `src/App.css`（修正）

### 追加機能

#### 1. インボイスデータモデル（仕訳に `invoice` フィールドを追加）
```js
invoice: {
  enabled:      boolean,   // false = インボイス情報なし
  invoiceNo:    string,    // T + 13桁（適格のみ）
  qualified:    boolean,   // true = 適格 / false = 非適格
  taxCategory:  string,    // TAX10 | TAX8 | EXEMPT | NON | FREE
  taxInput:     string,    // 'inclusive'（税込）| 'exclusive'（税抜）
  transitional: string,    // TRANS80 | TRANS50 | NO_CREDIT（非適格+課税のみ）
  baseAmount:   number,    // 税抜金額
  taxAmount:    number,    // 消費税額
  totalAmount:  number,    // 税込金額
  creditAmount: number,    // 仕入税額控除額
}
```

#### 2. 消費税区分
| コード | 名称 | 税率 |
|--------|------|------|
| TAX10  | 課税10%（標準税率） | 10% |
| TAX8   | 課税8%（軽減税率）  | 8%  |
| EXEMPT | 非課税             | 0%  |
| NON    | 不課税             | 0%  |
| FREE   | 免税               | 0%  |

#### 3. 経過措置（非適格 + 課税の場合のみ選択）
| コード | 名称 | 控除率 |
|--------|------|--------|
| TRANS80   | 経過措置80%控除（〜2026年9月） | 80% |
| TRANS50   | 経過措置50%控除（〜2029年9月） | 50% |
| NO_CREDIT | 控除不可                      | 0%  |

#### 4. 消費税計算ロジック
- 税込入力: `base = floor(amount × 100 / (100 + rate×100))`, `tax = amount - base`
- 税抜入力: `tax = floor(amount × rate)`, `total = amount + tax`

#### 5. 役割別アクセス
- 一般ユーザー: インボイス集計画面へのアクセス不可
- 承認者・管理者: インボイス集計画面にアクセス可能

#### 6. インボイス集計画面 `/invoice`
- 適格/非適格 × 消費税区分 × 経過措置でグループ集計
- 仕入税額控除額の自動計算・表示
- 集計CSV・明細CSVの2種類エクスポート

---

## 変更④ 電子帳簿保存法対応（2026-06-07）

### 対象ファイル
- `src/hooks/useAuditLog.js`（新規）
- `src/components/AuditHistory.jsx`（新規）
- `src/components/JournalEntry.jsx`（修正）
- `src/components/JournalList.jsx`（修正）
- `src/App.jsx`（修正）
- `src/App.css`（修正）

### 追加機能

#### 1. 訂正削除履歴の記録（useAuditLog.js）
- 仕訳の新規登録・修正・削除をすべて記録する `useAuditLog` フックを追加
- localStorage キー: `accounting_audit_log_v1`
- データ形式:
  ```js
  {
    id:         string,    // AL-YYYYMMDDHHMMSS-001
    type:       'CREATE' | 'EDIT' | 'DELETE',
    journalId:  string,    // 対象の伝票番号
    userId:     string,
    userName:   string,
    recordedAt: string,    // ISO 8601 UTC
    before:     object | null,  // 変更前スナップショット
    after:      object | null,  // 変更後スナップショット
  }
  ```
- `addLog()` を App.jsx の `handleSave` / `handleDelete` で呼び出し

#### 2. 仕訳編集画面に変更履歴タブ（JournalEntry.jsx）
- 編集時のみ「仕訳入力」「変更履歴」タブを表示
- 変更履歴タブ: 日時・操作者・操作種別を時系列表示
- 差分（日付・摘要・金額等）を 変更前 → 変更後 で強調表示
- `auditLogs` プロップで App.jsx から渡された履歴を表示

#### 3. 検索要件の強化（JournalList.jsx）
- 「取引先・行摘要」専用フィールドを追加（仕訳明細の `memo` 列を対象）
- 既存の日付範囲・金額範囲フィルターを継続活用

#### 4. 訂正削除履歴画面（AuditHistory.jsx）
- 全操作ログ（新規登録/修正/削除）を一覧表示
- フィルタ: 操作日範囲・操作種別・伝票番号キーワード
- 行展開で変更前/後の比較を表示
- CSV出力対応

#### 5. ナビゲーション・権限
- 「訂正削除履歴」ページ: `APPROVER` / `ADMIN` のみアクセス可能
- `ALL_MAIN_PAGES` に追加、`ALLOWED_PAGES` を更新

#### 6. 電子帳簿保存法対応バッジ（フッター）
- `app-footer` を `app-shell` に追加（印刷時非表示）
- 「電子帳簿保存法対応」グリーンバッジを常時表示

---

## 変更⑤: J-SOX 内部統制対応（2026-06-07）

### 目的
会計システムへの J-SOX（内部統制報告制度）対応。操作の完全な追跡可能性・承認ワークフローの強化・マスタ変更の証跡管理を実現。

### 追加・変更ファイル

#### 1. 操作ログ記録（src/hooks/useOpLog.js）
- localStorage キー: `accounting_oplog_v1`（最大 10,000 件、FIFO）
- ログ種別: LOGIN / LOGOUT / CREATE / UPDATE / DELETE / APPROVE / REJECT / EXPORT
- スキーマ:
  ```
  {
    id:        string,   // OP-<timestamp>
    type:      string,   // OP_TYPES のキー
    userId:    string,
    userName:  string,
    target:    string,   // 'journal:ID', 'auth', 'account:CODE' など
    detail:    string,   // 人間可読な説明
    at:        string,   // ISO 8601 UTC
  }
  ```

#### 2. 操作ログ照会画面（src/components/OpLog.jsx）
- 管理者（ADMIN）専用ページ（`oplog`）
- フィルタ: 日付範囲・操作者・操作種別・対象カテゴリ
- CSV エクスポート対応
- `TARGET_LABELS` でターゲットプレフィックスを日本語表示

#### 3. 内部統制ダッシュボード（src/components/ICSoxDashboard.jsx）
- 管理者（ADMIN）専用ページ（`jsoxdash`）
- KPI カード 4 種: 未承認仕訳数 / 今日の操作数 / 今月の操作ログ / 今月のマスタ変更数
- アラート: 3 日超の未承認仕訳 / 100 万円超の下書き仕訳
- 直近 7 日間のマスタ変更一覧
- クイックアクセスボタン（承認受付・操作ログ・訂正削除履歴・ユーザー管理）

#### 4. 承認ワークフロー強化（ApprovalInbox.jsx / useApprovals.js）
- 承認時にモーダルを表示し、任意の承認コメントを入力可能
- `approveJournal(approvalId, userId, comment)` に `comment` パラメータ追加
- 承認コメントを `approveComment` フィールドとして保存
- 承認履歴 CSV エクスポート追加（ステータス/伝票/金額/申請者/審査者/コメント）
- 自己承認防止: `isSelf` 判定で「自己承認不可」表示

#### 5. マスタ変更履歴の強化（AccountMaster / DepartmentMaster / UserManagement）
- 追加/変更時に「変更理由」テキストエリア入力を必須化
- 削除時は `window.prompt()` で削除理由を取得（必須）
- すべての操作を `logOp()` を通じて操作ログに記録
- `logOp` プロップを App.jsx から各マスタコンポーネントへ渡す

#### 6. App.jsx 統合
- `useOpLog` フック追加
- `logOp(entry)` ラッパー関数: `currentUser` からの userId/userName を自動補完
- `handleLogin` ラッパー: ログイン成功時に LOGIN オペレーションをログ記録
- `handleLogout` ラッパー: LOGOUT ログを記録してから `logout()` 呼び出し
- `handleSave` / `handleDelete`: 既存の audit ログに加えて操作ログも記録
- `handleApproveJournal` / `handleRejectJournal`: 承認/却下操作をログ記録
- ナビゲーションに「内部統制」ドロップダウン追加（ADMIN のみ）
- フッターに「J-SOX内部統制対応」バッジ追加（青色）

#### 7. ALLOWED_PAGES 更新
- `jsoxdash` / `oplog`: ADMIN のみアクセス可能

---

## 変更⑥: 連結会計機能追加（2026-06-07）

### 目的
グループ会社を持つ企業向けに、子会社データの収集・為替換算・相殺消去・連結財務諸表作成を一元管理できる連結会計機能を実装。

### 追加ファイル

#### 1. hooks/useSubsidiaries.js
- localStorage キー: `accounting_subsidiaries_v1`
- 子会社 CRUD（saveSubsidiary / deleteSubsidiary）
- `CURRENCIES`: JPY/USD/EUR/CNY/KRW/TWD/SGD/HKD/THB

#### 2. hooks/useConsolidation.js
- FX レート: `accounting_fxrates_v1`
- 連結パッケージ: `accounting_conso_packages_v1`
- 相殺消去: `accounting_eliminations_v1`
- `PKG_STATUS`: DRAFT / SUBMITTED / APPROVED / CONFIRMED
- `ACCOUNT_TYPES`: BS_ASSET / BS_LIABILITY / BS_EQUITY / PL_REVENUE / PL_EXPENSE
- `rateTypeForAccount()`: BS→期末レート、PL→平均レート
- `getFxRate(yearMonth, currency)`: JPY の場合 {endRate:1, avgRate:1} を返す

#### 3. components/consolidation/SubsidiaryMaster.jsx
- 子会社の登録・編集・削除（会社コード・名称・機能通貨・持分比率・所在国・状態）

#### 4. components/consolidation/FxRates.jsx
- 月次為替レートの登録（年月・通貨・期末レート・平均レート）
- BS科目に期末レート、PL科目に平均レートを適用する説明を表示
- 年月フィルタ・CSV出力対応

#### 5. components/consolidation/ConsolidationPackage.jsx
- 子会社・年月を選択してパッケージを作成・編集
- 科目区分（BS/PL）・科目コード・科目名・外貨金額を入力
- 為替レートで自動換算して JPY 表示（レート未登録の場合は警告表示）
- ステータス管理: 下書き保存 / 提出 / 承認 / 確定
- 提出期限の設定
- CSV出力対応

#### 6. components/consolidation/Eliminations.jsx
- 内部取引の相殺消去仕訳の登録・編集・削除
- 種別: 債権債務消去 / 売上高・仕入消去 / その他
- 借方・貸方それぞれに会社・科目コード・科目名を設定
- 自動生成ボタン（雛形を作成してユーザーが金額を調整）
- 年月フィルタ・CSV出力対応

#### 7. components/consolidation/ConsolidatedFS.jsx
- 損益計算書・貸借対照表の2タブ切り替え
- 各科目に「親会社」「子会社合計」「相殺消去」「連結合計」の4列表示
- 親会社データ: journals を年月フィルタして getPLSummary / getBSSummary で集計
- 子会社データ: APPROVED/CONFIRMED パッケージの amountJpy を accountType でマッピング
- 消去適用: RECEIVABLE_PAYABLE → 資産・負債から控除、REVENUE_EXPENSE → 収益・費用から控除
- 子会社内訳表示チェックボックス（各科目に子会社別金額を展開）
- 印刷・CSV出力対応
- 未確定パッケージ件数の警告表示

### 変更ファイル

#### App.jsx
- `useSubsidiaries`, `useConsolidation` フック追加
- 連結会計5画面のルーティング追加
- `CONSO_PAGES` 定義 + `isConsoPage` 判定
- ナビに「連結会計」ドロップダウン（ADMIN専用）追加
- `ALLOWED_PAGES.ADMIN` に `conso-*` ページを追加

#### App.css
- 通貨バッジ・持分比率バッジ・パッケージステータスバッジ
- 連結パッケージ選択・ステータスバー・アクション
- 相殺消去バッジ・借貸方グリッド
- 連結財務諸表レイアウト（4列表、消去赤字、純利益ハイライト）
